---
'@stellar-vue-ui/cli': minor
---

Generate correct Tailwind v4 theme CSS during init. The CLI now produces `@theme` blocks with `--color-*` tokens, dark mode overrides, keyframes, and `@utility` blocks so components render correctly out of the box.
